# academic-cv-projects-docx-exporter
Convert project data from a JSON file into a well-formatted Word (DOCX) document. The script reads JSON records, sorts and groups them, then generates a styled DOCX output with bold project titles, indented details, and consistent typography—ready to paste directly into a CV or research portfolio.
